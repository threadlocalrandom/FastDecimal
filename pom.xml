<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.tlr</groupId>
    <artifactId>fast-decimal-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>fast-decimal-core</module>
        <module>fast-decimal-vector</module>
        <module>fast-decimal-benchmarks</module>
    </modules>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Benchmark Settings -->
        <skipBenchmarks>true</skipBenchmarks>

        <!-- Dependency Versions -->
        <junit-jupiter.version>5.10.0</junit-jupiter.version>
        <jmh-core.version>1.37</jmh-core.version>
        <jmh-generator.version>1.37</jmh-generator.version>

        <!-- Plugin Versions -->
        <maven-surefire-plugin.version>3.1.2</maven-surefire-plugin.version>
        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <maven-shade-plugin.version>3.5.0</maven-shade-plugin.version>
        <jmh-maven-plugin.version>0.2.1</jmh-maven-plugin.version>
        <exec-maven-plugin.version>3.1.0</exec-maven-plugin.version>

        <!-- Benchmark Properties -->
        <benchmark.includes>.*Benchmark.*</benchmark.includes>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <argLine>--enable-native-access=ALL-UNNAMED</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
            </plugin>
        </plugins>
    </build>

    <!-- 
    Profiles section

    benchmark profile: Used to run JMH benchmarks
    Usage: 
      - Run benchmarks in a specific module: cd module-name && mvn test -Dbenchmark
      - Run specific benchmarks: cd module-name && mvn test -Dbenchmark -Dbenchmark.includes="FastDecimalBenchmark"
      - Run specific methods: cd module-name && mvn test -Dbenchmark -Dbenchmark.includes=".*add.*"
    -->
    <profiles>
        <profile>
            <id>benchmark</id>
            <activation>
                <property>
                    <name>benchmark</name>
                </property>
            </activation>
            <properties>
                <skipBenchmarks>false</skipBenchmarks>
            </properties>
        </profile>
    </profiles>

</project>
